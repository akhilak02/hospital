git config --global user.name "username"
git config --global user.name "email"


git init
git add .
git remote add origin "url"
git commit -m "command label"
git push -u origin master

git branch branch-name
git status
git checkout branch-name
cd.. for exit
git clone "url"
git pull "url"
